packages:
  # This is a list of packages that dbt will install and make available to your
  # project. You can add packages from the dbt Hub (https://hub.getdbt.com/) or
  # from any git repository.
  - package: dbt-labs/dbt_utils
    version: 1.3.0
